import styled from "styled-components/native";
import { flex, color, space, layout } from "styled-system";

const Box = styled.View`
  ${flex};
  ${color};
  ${space};
  ${layout};
`
export default Box;